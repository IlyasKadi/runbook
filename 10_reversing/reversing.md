# reversing

For reversing an executable `run` do

- Run the executable.
```shell
./run
```
```shell
./run --help
```
- Check for strings
```shell
strings run
```
- Record dynamic library calls
```shell
ltrace run
```
